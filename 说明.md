##### main分支和aaa分支

main分支上传后直接在netlify部署，每上传一次便花费一些信用额度，一个月只有20次，但它在国内访问似乎不用加速器。aaa分支用于netlify的预览部署，这不花费额度。
本月netlify信用额度用完了，甚至连接不上aaa分支，下个月再了解netlify预览部署并与vercel协调。（目前netlify提示服务器内部错误中）

##### 本地调试命令

(以防万一所以写在这里)

(本地可以与supabase数据库交互，不用上vercel.app测试)

```
node index.js
npm start
```

