```C
{
    config/{
        supabaseClient.js	//连supabase数据库
    }
    dadt/{
        comments.json	//自动创建用于保存评论，但有了数据库后有没有用这个
    }
    middlewares/{
        authMiddleware.js	//保护需登录认证的接口
    }
    node_modules/{……}
    public/{
        index1.html
        css/{
        	style.css
        }
        images/{……}
        js/{
        	app.js		//登录+评论，但目前网页中没有登录页面，先标记，之后解决
            main.js
        }
    }
    routes/{
    	auth.js		//与supabase数据库交互验证登录，但目前没法登录，先标记之后解决
    	comments.js		//评论功能，已更新，但内部有参数未使用
    }
    .env		//supabase设置
    index.js	//主文件
    package.json
    package-lock.json
    vercel.json		//请求全返回给index了
    .md		//说明文件，说明这是个说明文件
}
```

