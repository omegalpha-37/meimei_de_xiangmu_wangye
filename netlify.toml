[build]
  command = "npm install && npm run build"  # 或 "yarn install"
  publish = "."       

[functions]
  directory = "."

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server/:splat"
  status = 200
